{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "setup": "cd esp-geteaway && npm install && cd .. && cd device && npm install && cd .. && cd config.api && npm install && cd .. && cd es3 && npm install"
    },
    "dependencies": {
        "aws-sdk": "^2.583.0",
        "axios": "^0.19.0",
        "body-parser": "^1.19.0",
        "btoa": "^1.2.1",
        "cron": "^1.7.2",
        "express": "^4.17.1",
        "moment": "^2.24.0",
        "node-cmd": "^3.0.0",
        "rpio": "^1.3.0"
    }
}
